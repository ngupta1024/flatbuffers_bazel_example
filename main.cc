// main.cc
#include <iostream>
// #include "dummy_generated.h"

int main() {
    // flatbuffers::FlatBufferBuilder builder;
    // auto name = builder.CreateString("John");
    // auto dummy = CreateDummy(builder, 123, name);
    // builder.Finish(dummy);

    // const uint8_t* buffer = builder.GetBufferPointer();
    // int size = builder.GetSize();

    // // Accessing fields of the FlatBuffer
    // auto dummy_obj = GetDummy(buffer);
    // std::cout << "ID: " << dummy_obj->id() << std::endl;
    // std::cout << "Name: " << dummy_obj->name()->str() << std::endl;

    std::cout<<"naman's file"<<std::endl;
    return 0;
}
